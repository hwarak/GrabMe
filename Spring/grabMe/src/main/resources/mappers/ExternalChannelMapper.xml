<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.grabme.mappers.ExternalChannelMapper">
	
	<!-- insert shop url -->
	<insert id="insertURL">
		insert into externalChannel(shop_idx,openkatalkURL,instaURL) value(#{shop_idx},#{openkatalkURL},#{instaURL});
	</insert>
	
</mapper>