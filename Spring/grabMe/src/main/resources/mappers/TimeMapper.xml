<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.grabme.mappers.TimeMapper">

	<!-- insert time -->
	<insert id="insertTime">
		insert into time(shop_idx,time_date,time_time,time_people,time_max_people) 
		value(#{shop_idx},#{time_date},#{time_time},#{time_people},#{time_max_people})
	</insert>
	
	<!-- update time -->
	<update id="updateTime">
		update time set time_time=#{time_time} where time_idx=#{time_idx} 
	</update>
	
	<!-- delete time -->
	<delete id="deleteTime">
		delete from time where time_idx=#{time_idx}
	</delete>

	<!-- update time people Minus -->
	<update id="updateTimePeopleMinus">
		update time set time_people = time_people-1 where time_idx=#{time_idx}
	</update>
	
	<!-- update time people Plus -->
	<update id="updateTimePeoplePlus">
		update time set time_people = time_people+1 where time_idx=#{time_idx}
	</update>
	
	<!-- update time status -->
	<update id="updateTimeStatus">
		update time set time_status = if (time_people > 0, 1, 0), time_del_status = if (time_people != time_max_people, 0, 1) 
		where time_idx=#{time_idx}
	</update>
	
	<!-- select date > list -->
	<select id="selectDate" resultType="com.grabme.vo.TimeVO">
		select *, if(time_status , 'true', 'false') as time_status, if (time_del_status , 'true', 'false') as time_del_status 
		from time where shop_idx=#{shop_idx} and time_date=#{time_date} order by time_time asc;
	</select>


</mapper>